package com.careerdevs.intro;

public class DataTypes {
    public static void main(String[] args) {
  // primitive types for storing simple values
        //reference types for storing more complex values
        // we can use a under score instead of a comma when storing values
        // so when we are using Char characters we will use single quotes when identifying a string we use double quotes
 // reference types are copied by the reference while primitive types are copied by the value    }
}
